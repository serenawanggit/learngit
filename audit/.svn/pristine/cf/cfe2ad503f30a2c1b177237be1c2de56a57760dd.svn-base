package audit.controller;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

@Controller
public class SupportFileController {

	//private SupportFileI supportFileService;
	//@Autowired
//	public void setSupportFileService(SupportFileI supportService){
		//this.supportFileService = supportService;
	//}
	@RequestMapping(value="/supportFileController/findSupportFile")
	@ResponseBody
	public String find(HttpServletRequest request,HttpServletResponse response){
//		String id = request.getParameter("id");
//		Support_File supportFile = supportFileService.findSupportFileById(Integer.parseInt(id));
//		String url = supportFile.getUrl();
//		String name = url.substring(0,url.lastIndexOf("."));
		String message = "/preview/";
		return message;
	}
}
